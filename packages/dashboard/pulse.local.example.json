{
  "$schema": "./pulse.config.json",
  "_comment": "Copy this file to pulse.local.json and fill it in. pulse.local.json is gitignored.",

  "network": "sepolia",
  "rpc": {
    "_comment": "Public client RPC. You can paste your full Alchemy URL here, OR put just the API key in env (NEXT_PUBLIC_ALCHEMY_KEY).",
    "alchemyKey": "REPLACE_WITH_YOUR_ALCHEMY_KEY",
    "fullUrlOverride": null
  },

  "walletConnectProjectId": "REPLACE_WITH_YOUR_WALLETCONNECT_PROJECT_ID",

  "contracts": {
    "_comment": "Fill these in after deploying. `manager` and `payrollManager` use the subscribe/addRecipient + cancel flows; `executor` is the PulseExecutor router that the off-chain bot calls (executor.execute(paymentId)). The manager's trustedExecutor must be set to this executor address — DeployExecutorSepolia.s.sol wires that up.",
    "manager": "0x0000000000000000000000000000000000000000",
    "payrollManager": "0x0000000000000000000000000000000000000000",
    "executor": "0x0000000000000000000000000000000000000000",
    "usdc": "0x0000000000000000000000000000000000000000",
    "feeRecipient": "0x0000000000000000000000000000000000000000"
  },

  "_deploymentBlockNote": "Sepolia block at/just before the manager deployment. Used as the start of the eth_getLogs scan so we don't crawl the whole chain. Alchemy/Etherscan show this number in your deploy tx.",
  "deploymentBlock": 0,

  "merchant": {
    "_comment": "Your MetaMask address. Used as the default 'merchant' and shown in the UI as the connected merchant.",
    "address": "0x0000000000000000000000000000000000000000",
    "label": "My Merchant"
  },

  "executor": {
    "_comment": "Server-side private key used by the off-chain bot to call PulseExecutor.execute(paymentId) on Sepolia. Should be a dedicated EOA funded with a small amount of Sepolia ETH for gas — NEVER a key with real funds. Prefer setting it in .env.local as EXECUTOR_PRIVATE_KEY to keep it out of JSON. The bot wallet receives the executor fee (minus any penalty) for each successful call.",
    "privateKey": null
  },

  "testAddresses": [
    {
      "_comment": "Optional extras to show in the UI dropdowns — e.g. a second MetaMask account you use as a customer.",
      "label": "My Customer Wallet",
      "address": "0x0000000000000000000000000000000000000000"
    }
  ]
}
